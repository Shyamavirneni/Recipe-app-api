

services:
  app:
    build:
      context: .
      args:
         Dev: "true"
    ports:
        - "8001:8000"
    volumes:
      - .:/app
    command: >
      sh -c "python manage.py runserver 0.0.0:8000"
